[project]
name = "spotify-api-data-pipeline"
version = "0.1.0"
description = ""
authors = [
    {name = "woramate-pongsa",email = "woramate.pong@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt (>=1.0.0.40.14,<2.0.0.0.0)",
    "dbt-core (>=1.11.2,<2.0.0)",
    "dbt-redshift (>=1.10.0,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
